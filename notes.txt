rebar3 release 
_build/default/rel/erwaf/bin/erwaf console
---
TODO
[x] XSS-analyzer 
[x] SQLI-analyzer
    [x] check Form fields;
    [x] GET, POST, HEAD, PUT, DELETE: validate common requests; 
    [ ] match each part: path, body, cookies, user-agent, etc.
    [ ] block junk requests: non-standard method; non-ANSI
[x] Typings
[x] Configure release: compile, run demo, add commands into README
[x] Compiled RegEx in a standalone module
[ ] Parallel processing of parts of a single request: path, headers, body, etc.
[ ] Rewrite parser_service.erl
[ ] Run supervisor and link to app 
[ ] Logging score (logger file | ClickHouse)
--
[ ] Reverse proxy:
    [ ] Valid: Pass to API server
    [ ] Invalid: reject
[ ] Restart process on error; verify it restarts
[ ] Download raw HTTP-requests (normal and malicious) for testing
[ ] Create unit tests
[ ] Make Docker container for demo
[ ] ci/cd
[ ] Integrate with a process written in Python (or any other lang)

---

% /*
% Pipeline
% 1 have http request; pass it to general Parser
% 2 parse http request body and extract data: {method, cookies, body, URL, params, ...}
% 3 pass the extracted data to respective independent Analyzers (processes); get AnalyzerResponse: {ok|block|Reason|Score}; fail-closed policy of analyzer
% 4 pass AnalyzerResponse to DecisionMaker: all valid - pass, 1 invalid - block; pass {decision} to ResponseManager
% 5 ResponseManager (valid|invalid) returns HTTP 403 Forbidden to Client or passes initial http_requst (not the parsed and reconstructed one) next to api server.
% 6 ResponseManager receives responses from api server and passes to Client
% Monitor observe each step, logs data, helps false-positive detection.
% Logging & audit.
% */

---

TODO: handle
http -v localhost:8081/./../../etc/password
http -v --path-as-is localhost:8081/./../../etc/password

### Testing in shell
Run in a terminal
% rebar3 shell

Run in another terminal
Good requests:
http -v GET localhost:8081/api/v1/users/123 'hello=world'
http -v -f POST 'localhost:8081/api/v1/post?id=123' hello=World
http -v DELETE localhost:8081/item/1
http -v PUT localhost:8081/put name=John email=john@example.org
http -v PUT localhost:8081/put \
    name=John \
    age:=29 \
    married:=false \
    hobbies:='["http", "pies"]' \
    favorite:='{"tool": "HTTPie"}' \
    bookmarks:=@./data.json \
    description=@./notes.txt
http -v HEAD "localhost:8081/"

Bad requests:
GET with SQLi in path and body:
http -v GET localhost:8081/api/'SELECT * from U;--' 'username=Select * from Users;--'
http -v GET localhost:8081/api/'select * from U;--' "username=UPDATE users SET pass = '1' where user = 't1' OR 1=1--"
http -v GET localhost:8081/api/"username=SELECT * from table where id = 1 union select 1,2,3"
http -v GET "localhost:8081/user/admin’ OR ‘1’=’1"
http -v -f POST localhost:8081/post/'userid=SELECT * from Users where 1=1;--' "username=SELECT * from table where id = 1 union select 1,2,3"
http -v DELETE localhost:8081/item/'Select * From Items where 1=1;--'
http -v PUT localhost:8081/put name='DROP TABLE Users;' email=john@example.org
http -v GET "localhost:8081/query=<script>alert(1)</script>"
http -v HEAD "localhost:8081/'Select * From Items where 1=1;--'"